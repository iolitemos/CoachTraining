// Default (Development) environment. Replaced per build configuration via
// angular.json "fileReplacements" (see environment.qas.ts / environment.production.ts).
export const environment = {
  envName: 'DEV',
  production: false,
  apiBaseUrl: 'http://localhost:5183/api',
};
